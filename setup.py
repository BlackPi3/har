from setuptools import setup, find_packages

setup(
    name="har-project",
    version="0.1.0",
    description="Human Activity Recognition project",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[
        # Add your main dependencies here if you want
        # "torch",
        # "numpy", 
        # "matplotlib",
        # "pyyaml",
    ],
)
