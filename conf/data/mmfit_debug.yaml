defaults:
  - mmfit

# Override only what speeds up iteration
train_subjects: ["w01"]
val_subjects: ["w14"]
test_subjects: ["w09"]

debug_subset: true
debug_limit_per_split: 16

batch_size: 16
num_workers: 2

# Note: use with `data=mmfit_debug trainer.epochs=2`