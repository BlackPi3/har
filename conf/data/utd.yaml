dataset_name: utd

pose_file: skeleton_aligned.npy
acc_file: inertial_std.npy

# Subject splits (leave-one-subject-out style)
train_subjects: ["s1", "s2", "s3", "s4"]
val_subjects: ["s5", "s6"]
test_subjects: ["s5", "s6"] #["s7", "s8"]

# Sampling + window policy (50 Hz, 2-second window)
sampling_rate_hz: 50
sensor_window_length: 100
stride_seconds: 0.1
batch_size: 64
