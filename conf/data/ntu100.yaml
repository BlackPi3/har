dataset_name: ntu

pose_file: pose_100hz.npy 
acc_file: None

# Subject splits (leave-one-subject-out style)
train_subjects:
val_subjects:
test_subjects:

# Sampling + window policy (50 Hz, 2-second window)
sampling_rate_hz: 100
sensor_window_length: 250
stride_seconds: 0.2
batch_size: 128
