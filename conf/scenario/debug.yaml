defaults:
  - scenario2
  - _self_

# Debug-focused overrides (fast iteration)
experiment_name: debug_scenario2
trainer:
  epochs: 2
  patience: 2

# Suggested usage: data=mmfit_debug scenario=debug trainer.epochs=2
